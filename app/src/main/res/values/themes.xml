<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SoloBambas" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">#0066FF</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondary">#FF6D00</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
    </style>

    <style name="Theme.SoloBambas" parent="Base.Theme.SoloBambas" />

    <!-- Tema para el Splash Screen -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    
    <!-- Tema para el Splash Screen inicial de la aplicación -->
    <style name="AppSplashTheme" parent="SplashTheme">
        <item name="android:windowBackground">@drawable/app_splash_background</item>
    </style>
</resources>